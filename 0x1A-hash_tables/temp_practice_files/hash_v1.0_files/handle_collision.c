#include "hash.h"

/**
 * handle_collision - a function that handles collision when adding elements
 *                    to a hash table
 * @table: a hash table
 * @index: the index to which the element is to be added
 * @item: the item to add at index
 * Return: void
 */
void handle_collision(HashTable *table, unsigned long index, Ht_item *item)
{
	(void)table;
	(void)index;
	(void)item;
}
